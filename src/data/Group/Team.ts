import { GuildMember, Role, User } from "discord.js";
import BotSystem from "../BotSystem";

export default class Team {
    
    async createTeam(botSystem: BotSystem, groupName: string) {

    }

    async sendInvite(role: Role, user: GuildMember) {

    }
}